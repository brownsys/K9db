load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "memcached",
    srcs = [
        "encode.h",
        "main.cc",
        "mariadb.h",
        "memcached.h",
    ],
    linkopts = [
        "-lmariadbcpp",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "@libmemcached",
    ],
)
