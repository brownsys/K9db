#include "simple_cppstring.h"
#include <string>

std::string query_pelton(std::string query) {
    // set response from pelton (i.e. table data)
    std::string response = "query response";
    // send response back to rust proxy
    return response;
}